name: 🪲 Bug 报告 (请优先使用英文版本)
description: 报告一个需要修复的问题
labels:
  - bug
  - needs triage
body:
  - type: markdown
    attributes:
      value: |
        感谢您提交错误报告！它有助于让 Codexia 变得更好。

        如果您需要使用 Codexia 的帮助或支持，而不是报告错误，请在 [codexia/discussions](https://github.com/milisp/codexia/discussions) 上发帖，您可以在那里提问或与他人交流改进 Codexia 的想法。

        请确保您正在运行 [最新](https://milisp.github.io/modern-github-release/#/repo/milisp/codexia) 版本的 Codexia 和 [Codex CLI](https://github.com/openai/codex/releases)。您遇到的错误可能已经修复。

        请尽量包含尽可能多的信息。

  - type: input
    id: version
    attributes:
      label: 正在运行的 Codexia 版本是什么？
      description: 关于版本
    validations:
      required: true
  - type: input
    id: codex-version
    attributes:
      label: 正在运行的 Codex 版本是什么？
      description: 复制 `codex --version` 的输出
    validations:
      required: true
  - type: input
    id: provider
    attributes:
      label: 您使用的是哪个提供商？例如 `OpenAI`、`Gemini`、`Qwen` 等
    validations:
      required: true
  - type: input
    id: model
    attributes:
      label: 您使用的是哪个模型？
      description: 例如 `gpt-4.1`、`o4-mini`、`o3` 等。
  - type: input
    id: platform
    attributes:
      label: 您的计算机平台是什么？
      description: |
        对于 MacOS 和 Linux：复制 `uname -mprs` 的输出
        对于 Windows：复制 PowerShell 控制台中 `"$([Environment]::OSVersion | ForEach-Object VersionString) $(if ([Environment]::Is64BitOperatingSystem) { "x64" } else { "x86" })"` 的输出
  - type: textarea
    id: actual
    attributes:
      label: 您看到了什么问题？
      description: 请包含完整的错误消息和已编辑 PII 的提示。如果可能，请提供文本而不是屏幕截图。
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: 重现此错误的步骤是什么？
      description: 解释错误并提供可以重现它的信息。
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: 预期的行为是什么？
      description: 如果可能，请提供文本而不是屏幕截图。
  - type: textarea
    id: notes
    attributes:
      label: 附加信息
      description: 您认为我们还应该知道什么吗？
